{
  "name": "kanban-kukac",
  "version": "1.0.0",
  "main": "index.js",
  "description": "",
  "license": "ISC",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "clean:reports": "rimraf cypress/reports && rimraf cypress/screenshots && rimraf cypress/videos",
    "report:merge": "mochawesome-merge \"cypress/reports/*.json\" > cypress/reports/mochawesome.json",
    "report:gen": "marge cypress/reports/mochawesome.json -f index -o cypress/reports/html",
    "test": "npm run clean:reports && cypress run --reporter mochawesome --reporter-options reportDir=cypress/reports,overwrite=false,html=false,json=true && npm run report:merge && npm run report:gen",
    "test:html": "npm run test && echo Relatório: cypress/reports/html/index.html"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.0",
    "axe-core": "^4.10.3",
    "cypress": "^14.5.4",
    "cypress-axe": "^1.6.0",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.4.1",
    "mochawesome-report-generator": "^6.2.0",
    "rimraf": "^6.0.1"
  }
}
